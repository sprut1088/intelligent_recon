services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    environment:
      - PSR_AMOUNT_DIVISOR=1
      - MINOR_VARIANCE_TOLERANCE=50
  frontend:
    build: ./frontend
    ports: ["5173:80"]
    depends_on: [backend]
